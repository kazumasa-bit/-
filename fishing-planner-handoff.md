# 小田原 釣りプランナー ― 引き継ぎ資料

> このリポジトリに置いた `odawara-fishing-planner.html` 1ファイルで完結する、
> 相模湾・西湘エリア向けの「釣行プランナー」です。外部ライブラリ・外部画像なしで動きます。

---

## コピペ用プロンプト（Claude Code に貼る用）

```
あなたは「小田原 釣りプランナー」(odawara-fishing-planner.html) の開発を引き継ぎます。
この fishing-planner-handoff.md を必ず読み、§4「崩してはいけない設計判断」を厳守してください。
データモデル(§2: SPOTS / FISH / RIGS / TIDES / calcShio)を理解した上で作業します。
最初のタスクは §6 の【ここに着手したい項目を書く】です。
変更後はブラウザで開いて動作確認し、何を変えたかを簡潔に報告してください。
```

---

## 1. プロジェクト概要・現状の実装機能

- **形態**：単一HTML（`odawara-fishing-planner.html`）。JS/CSSすべて内包、外部依存ゼロ。オフラインでも開ける。
- **対象**：小田原・西湘・相模湾エリアの陸っぱり釣り。
- **実装済み機能**：
  1. **日付 → 潮まわり表示**：`calcShio()` が月齢から潮名（大潮/中潮/小潮/長潮/若潮）を**目安**として算出。色付きバッジで表示。
  2. **外部潮見表への誘導**：正確な時刻はアプリ内では出さず、「小田原 潮見表」の検索リンクへ。
  3. **釣り場一覧（SPOTS）**：自作SVGアイコン、狙える魚タグ、**禁止・注意の注記**、YouTube/釣果検索リンク。
  4. **魚の旬カレンダー（FISH）**：12か月のヒートマップ（旬度=推定）。選択中の月をハイライト。魚ごとの釣り方YouTubeリンク。
  5. **仕掛け一覧（RIGS）**：釣り方・狙える魚・解説。
  6. **ファミリーモード トグル**：家族・初心者向けの釣り場/仕掛け/魚を優先表示し、安全注意を表示。

---

## 2. データモデル

| 名前 | 型 | 役割 | 主なフィールド |
|---|---|---|---|
| `SPOTS` | 配列 | 釣り場 | `name, loc, family(bool), fish[], forbid(禁止注記・必須)` |
| `FISH` | 配列 | 魚種と旬 | `name, kind('fish'/'squid'), color, season[12](0〜3,推定), rig` |
| `RIGS` | 配列 | 仕掛け/釣り方 | `name, family(bool), target, desc` |
| `TIDES` | オブジェクト | 潮名の定義 | キー=潮名 → `{cls(CSSクラス), desc}` |
| `calcShio(date)` | 関数 | 日付→潮名(目安) | 月齢を朔望月29.53で算出→旧暦日≒月齢+1→潮名にマップ。戻り値 `{name, age, lunarDay}` |

- SVGヘルパー：`fishSVG(color)` / `squidSVG(color)` / `spotSVG()`（すべて自作インラインSVG）。
- 状態：`state = { date, family }`。`renderAll()` が全セクションを再描画。

---

## 3. データの増やし方（よくある編集）

- **釣り場を足す** → `SPOTS` に1要素追加。`forbid` は**必ず**書く（空にしない）。
- **魚を足す** → `FISH` に追加。`season` は1〜12月の12要素（0=オフ〜3=よく狙える、推定でよい）。`kind:"squid"` でイカ系SVG。
- **仕掛けを足す** → `RIGS` に追加。家族向けなら `family:true`。

---

## 4. 崩してはいけない設計判断

1. **外部写真はNG → 自作SVGで表現**。画像URLの読み込みを足さない。
2. **潮名は「目安」。満潮・干潮の正確な時刻は出さない**（外部潮見表へ誘導する方針を維持）。
3. **釣り場の「釣り禁止・立入禁止」注記（`forbid`）は消さない・薄めない**。常に目立つ赤系で表示。
4. **旬度（`season`）は推定**。確定情報のように見せない（「推定」ラベルを残す）。

---

## 5. 情報の出典・前提

- 釣り場・魚種・旬・仕掛けは、相模湾／西湘エリアの一般的な傾向に基づく**整理・推定**です（特定の有料データの転載ではありません）。
- 潮名は月齢からの簡易推定で、地域・年によりズレます。
- 規制・遊漁ルール・立入可否は変わります。**実際の釣行前に公式・現地表示で必ず確認**してください。

---

## 6. バックログ（次の候補タスク）

- [ ] **潮見表/釣果リンクの精度向上**：検索リンク → 小田原ピンポイントの潮汐ページへ。
- [x] YouTube検索リンク（釣り場・魚ごと）※実装済み
- [x] ファミリートグル ※実装済み
- [ ] **データの外部化**：`SPOTS/FISH/RIGS` を別JSON化して読み込む。
- [ ] **PWA化**：manifest + Service Worker でオフライン保存・ホーム追加。
- [ ] **天気・波予報リンク**：エリア別の気象/波浪情報への導線。
- [ ] **「今おすすめ」サマリ**：選択日の潮＋その月の旬から、狙い目の魚×釣り場×仕掛けを提案。
- [ ] **釣果メモ**：localStorage に簡易記録。

---

## 7. 動作確認

- ブラウザで `odawara-fishing-planner.html` を直接開くだけ。サーバ不要。
- 確認ポイント：日付変更で潮名が変わる／ファミリーモードで並び替え＆注意文表示／各リンクが新規タブで開く／禁止注記が全釣り場に出る。
